= form_for @artist, remote: true do |f|
  = f.label :name, "Artist Name"
  = f.text_field :name
  = f.label :bio, "Bio"
  = f.text_area :bio, :rows => 2
  .text-center= f.submit "Submit", :class => "button"